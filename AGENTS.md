# 孫子兵法 十三篇 開発メモ

- 『孫子兵法』十三篇を、単なる翻訳ではなく、何度も読み返せる読み物として多層的に味わうための静的サイト。
- 世界観を最優先にする。心得や余話は読み方の一例として示し、特定の価値観や一つの解釈に寄せない。原文と書き下し文も、淡々とした味わいが伝わるように見せる。
- 読み物として純粋に読める状態を保つ。既読管理、チェックボックス、進捗、通読モードなど、読んだかどうかを気にさせる機能や閲覧記録の保存を加えない。使わなくなったデータやコードは残さず削除する。
- 各篇に同じ説明文を繰り返し載せない。進言の本文は文字サイズを揃える。アニメーションは控えめにし、動かす場合も素早く済ませる。

## 構成

- フレームワークや依存パッケージのない静的サイト。編集対象は `src/`、`dist/` は `bun run build` の生成物なので直接編集しない。
- データは `src/chapters.js`（篇と進言）、`src/full-texts.js`（原文・書き下し文）、`src/sayings.js`（名句）、`src/lessons.js`（心得と場面索引）、`src/asides.js`（余話）に分かれている。
- 原文・書き下し文と名句には出典を表示する。主な全文の参照先は中国語版・日本語版 Wikisource。
- `design/` は制作時の参考画像。

## 検証と反映

- 変更後は `bun run build` を通し、`bun run dev -- --host 127.0.0.1 --port 0` などで起動して、1440px と 420×912px で表示と操作を確認する。
- LocalWeb では `sonshi-guide.localhost`、Tailscale 経由では `/apps/sonshi-guide/` で参照されるため、パスは相対形式を保つ。`bun run build` で `dist/` を更新すればそのまま反映される。
